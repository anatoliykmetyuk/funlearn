
## Start

Migration: Launch Docker and run `./migrate.ps1`.

Live update incantation: from `sbt` console, `~funlearnJVM/reStop; funlearnJS/fastOptJS; funlearnJVM/reStart`.

VPN may interfere with local development, so disable it if there are issues connecting to the server.

On update of the build: `bloopGenerate`